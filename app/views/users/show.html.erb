<h4>users show</h4>
<div class="container">
  <div class="row">
    
    <!-- 左側 -->
    <%= render 'books/common', books: @books, user: @user %>
    
    <!-- 右側 -->
    <div class="col-md-8 offset-md-1">
      <h4>Books</h4>
        <table class="table table-striped">
          <table class="users_table">
            <tr>
              <th></th>
              <th>Title</th>
              <th>Opinion</th>
            </tr>
            <tbody>
              <% @books.each do |book| %>
                <tr>
                  
                  <td><%= link_to user_path(@user.id) do %>
                  <%= image_tag @user.get_profile_image %>
                  <% end %></td>
                  
                  <td><%=link_to book.title, book_path(book.id) %></td>
                  <td><%= book.body %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </table>
    </div> 
     
  </div>
</div>